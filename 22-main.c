#include <stdio.h>

/**
 * macro definition
 */
#define LIMIT 5

/**
 * main - introduction to macros
 * Return: Always (0);
 */
int main(void)
{
	printf("The value of limit is: %d\n", LIMIT);

	return (0);
}
